<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- Fullscreen Official PayU Checkout Web View -->
    <WebView
        android:id="@+id/web_view_checkout"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Sleek Top Progress Bar -->
    <ProgressBar
        android:id="@+id/progress_checkout"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_gravity="top"
        android:indeterminateTint="#E85D75"
        android:indeterminate="true" />

    <!-- Top Controls Floating Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp">

        <ImageButton
            android:id="@+id/btn_close_checkout"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="@drawable/bg_checkout_circle_btn"
            android:elevation="6dp"
            android:contentDescription="Close"
            android:src="@drawable/ic_close"
            app:tint="#444444" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <com.google.android.material.chip.Chip
            android:id="@+id/chip_mode_toggle"
            style="@style/Widget.Material3.Chip.Assist.Elevated"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📷 Scan QR / VPA"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#E85D75"
            android:elevation="6dp"
            app:chipBackgroundColor="#FFFFFF"
            app:chipStrokeColor="#FCE4EC"
            app:chipStrokeWidth="1dp" />
    </LinearLayout>

    <!-- Error Fallback View -->
    <LinearLayout
        android:id="@+id/layout_checkout_error"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        android:background="#FFFFFF"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Unable to load payment page"
            android:textColor="#C62828"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:text="Please check your internet connection and try again."
            android:textColor="#777777"
            android:textSize="13sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_retry_checkout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Retry"
            app:backgroundTint="#E85D75"
            app:cornerRadius="20dp" />
    </LinearLayout>
</FrameLayout>
