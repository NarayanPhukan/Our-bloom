<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_love_note_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_widget_glass"
    android:padding="16dp">

    <!-- Header Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🌸"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/tv_widget_note_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Daily Love Note"
            android:textColor="#FF4D6D"
            android:textSize="13sp"
            android:textStyle="bold"
            android:fontFamily="@font/playfair_display" />

        <!-- Quick Heartbeat Button -->
        <TextView
            android:id="@+id/btn_widget_heartbeat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Send ❤️"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:textStyle="bold"
            android:background="@drawable/bg_whatsapp_pill"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="3dp"
            android:paddingBottom="3dp" />
    </LinearLayout>

    <!-- Note Content -->
    <TextView
        android:id="@+id/tv_widget_note_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:maxLines="4"
        android:text="&quot;You make every single day feel like a garden in full bloom...&quot;"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:textStyle="italic"
        android:lineSpacingMultiplier="1.2" />

    <!-- Author & Date Attribution -->
    <TextView
        android:id="@+id/tv_widget_note_author"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:gravity="end"
        android:text="— With love ✨"
        android:textColor="#B0B7C3"
        android:textSize="11sp" />

</LinearLayout>
